html5 new feature
new input types in html5 forms
html5 graphics elements
semantic elements
metadata in html5
web components
geo location
manifest file 
web workers
web storage - local storage/ session storage / cookies
diff bet SVG & CANVAS
diff bet inline & block elements
iframe - nesting of webpage
doctype - n its types
meta tag, charset attribute
how we can display youtube video in html?
anchor tag vs link
	anchor tag is used to create a hyperlink to another webpage.
	link tag <link> defines a link between a document and an external resource and these are not clickable.
